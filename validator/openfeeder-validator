#!/usr/bin/env python3
"""OpenFeeder Validator — executable entry point."""

import os
import sys

# Allow running from the validator/ directory directly
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

from validator import main

if __name__ == "__main__":
    main()
